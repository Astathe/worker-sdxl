diff --git a/.runpod/hub.json b/.runpod/hub.json
index 05b28f0..17774cd 100644
--- a/.runpod/hub.json
+++ b/.runpod/hub.json
@@ -10,13 +10,10 @@
     "gpuIds": "ADA_24",
     "gpuCount": 1,
     "allowedCudaVersions": [
+      "12.9",
+      "12.8",
       "12.7",
-      "12.6",
-      "12.5",
-      "12.4",
-      "12.3",
-      "12.2",
-      "12.1"
+      "12.6"
     ]
   }
 }
diff --git a/.runpod/tests.json b/.runpod/tests.json
index 2e36e77..bc238e7 100644
--- a/.runpod/tests.json
+++ b/.runpod/tests.json
@@ -3,7 +3,6 @@
     {
       "input": {
         "workflow": {
-          "_comment": "Replace this with your full ComfyUI API-format workflow JSON. This is a minimal example placeholder.",
           "3": {
             "class_type": "KSampler",
             "inputs": {
@@ -96,13 +95,10 @@
     "gpuTypeId": "NVIDIA GeForce RTX 4090",
     "gpuCount": 1,
     "allowedCudaVersions": [
+      "12.9",
+      "12.8",
       "12.7",
-      "12.6",
-      "12.5",
-      "12.4",
-      "12.3",
-      "12.2",
-      "12.1"
+      "12.6"
     ]
   }
-}
\ No newline at end of file
+}
diff --git a/Dockerfile b/Dockerfile
index f15a223..d4c0f42 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -1,12 +1,11 @@
 # Start from the official RunPod worker-comfyui base image
-FROM runpod/worker-comfyui:5.1.0-base
+FROM runpod/worker-comfyui:5.8.5-base
 
 # =============================================================================
 # Install Custom Nodes
 # =============================================================================
 # We use the GitHub URLs to ensure we get exactly the repos previously installed.
 RUN comfy-node-install \
-    https://github.com/Comfy-Org/ComfyUI-Manager \
     https://github.com/ltdrdata/ComfyUI-Impact-Pack \
     https://github.com/ltdrdata/ComfyUI-Impact-Subpack \
     https://github.com/yolain/ComfyUI-Easy-Use \
diff --git a/start.sh b/start.sh
index 39ed08a..5e54812 100644
--- a/start.sh
+++ b/start.sh
@@ -1,45 +1,48 @@
 #!/bin/bash
-set -euo pipefail
+set -uo pipefail
 
 # =============================================================================
-# Startup script: Launch ComfyUI server, then start the RunPod handler
+# Startup script: launch ComfyUI in the background, wait for it, start handler
 # =============================================================================
 
 COMFY_DIR="${COMFY_DIR:-/comfyui}"
 
+# tcmalloc for better memory behaviour (same as the official worker-comfyui)
+TCMALLOC="$(ldconfig -p | grep -Po "libtcmalloc.so.\d" | head -n 1 || true)"
+if [ -n "$TCMALLOC" ]; then export LD_PRELOAD="$TCMALLOC"; fi
+
+# Keep ComfyUI-Manager from trying to reach the network on boot
+comfy-manager-set-mode offline 2>/dev/null || echo "[start.sh] Could not set ComfyUI-Manager to offline mode" >&2
+
 echo "[start.sh] Starting ComfyUI server..."
 cd "$COMFY_DIR"
-
-# Start ComfyUI in the background on localhost:8188
-python main.py \
+python -u main.py \
     --listen 127.0.0.1 \
     --port 8188 \
     --disable-auto-launch \
     --disable-metadata \
-    &
-
+    --log-stdout &
 COMFY_PID=$!
 echo "[start.sh] ComfyUI started with PID $COMFY_PID"
 
-# Wait for ComfyUI to be ready (poll /system_stats)
+# Wait for ComfyUI. The base image has no curl, so poll with Python instead.
 echo "[start.sh] Waiting for ComfyUI to be ready..."
-MAX_WAIT=300
+MAX_WAIT="${COMFY_STARTUP_TIMEOUT:-600}"
 WAITED=0
-while [ $WAITED -lt $MAX_WAIT ]; do
-    if curl -s http://127.0.0.1:8188/system_stats > /dev/null 2>&1; then
-        echo "[start.sh] ComfyUI is ready! (waited ${WAITED}s)"
-        break
+until python -c "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8188/system_stats', timeout=3)" 2>/dev/null; do
+    if ! kill -0 "$COMFY_PID" 2>/dev/null; then
+        echo "[start.sh] ERROR: ComfyUI process exited during startup. Check the logs above." >&2
+        exit 1
+    fi
+    if [ "$WAITED" -ge "$MAX_WAIT" ]; then
+        echo "[start.sh] ERROR: ComfyUI did not start within ${MAX_WAIT}s" >&2
+        exit 1
     fi
     sleep 2
     WAITED=$((WAITED + 2))
 done
+echo "[start.sh] ComfyUI is ready! (waited ${WAITED}s)"
 
-if [ $WAITED -ge $MAX_WAIT ]; then
-    echo "[start.sh] ERROR: ComfyUI did not start within ${MAX_WAIT}s"
-    exit 1
-fi
-
-# Start the RunPod handler
 echo "[start.sh] Starting RunPod handler..."
 cd /
-python -u /handler.py
+exec python -u /handler.py
diff --git a/test_input.json b/test_input.json
index 1fbdcb8..2dd2e03 100644
--- a/test_input.json
+++ b/test_input.json
@@ -1,7 +1,6 @@
 {
   "input": {
     "workflow": {
-      "_comment": "Replace this with your full ComfyUI API-format workflow JSON. This is a minimal example placeholder.",
       "3": {
         "class_type": "KSampler",
         "inputs": {
@@ -11,10 +10,22 @@
           "sampler_name": "euler",
           "scheduler": "normal",
           "denoise": 1.0,
-          "model": ["4", 0],
-          "positive": ["6", 0],
-          "negative": ["7", 0],
-          "latent_image": ["5", 0]
+          "model": [
+            "4",
+            0
+          ],
+          "positive": [
+            "6",
+            0
+          ],
+          "negative": [
+            "7",
+            0
+          ],
+          "latent_image": [
+            "5",
+            0
+          ]
         }
       },
       "4": {
@@ -35,28 +46,43 @@
         "class_type": "CLIPTextEncode",
         "inputs": {
           "text": "A majestic steampunk dragon soaring through a cloudy sky, intricate clockwork details, golden hour lighting, highly detailed",
-          "clip": ["4", 1]
+          "clip": [
+            "4",
+            1
+          ]
         }
       },
       "7": {
         "class_type": "CLIPTextEncode",
         "inputs": {
           "text": "blurry, low quality, deformed, ugly, text, watermark, signature",
-          "clip": ["4", 1]
+          "clip": [
+            "4",
+            1
+          ]
         }
       },
       "8": {
         "class_type": "VAEDecode",
         "inputs": {
-          "samples": ["3", 0],
-          "vae": ["4", 2]
+          "samples": [
+            "3",
+            0
+          ],
+          "vae": [
+            "4",
+            2
+          ]
         }
       },
       "9": {
         "class_type": "SaveImage",
         "inputs": {
           "filename_prefix": "ComfyUI",
-          "images": ["8", 0]
+          "images": [
+            "8",
+            0
+          ]
         }
       }
     }